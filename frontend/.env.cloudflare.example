# Frontend apontando para API via Cloudflare Tunnel (HTTPS).
# Copie: cp .env.cloudflare.example .env.local
# Ajuste o hostname da API e reinicie: npm run dev

NEXT_PUBLIC_API_URL=https://api-ia.SEU-DOMINIO.gov.br
